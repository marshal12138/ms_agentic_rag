# SPAD Stage2 资源监控报告

运行目录：
`log/agenticIterRag/260709-231614-279722-pipeline-agentic_iter_rag_v1_spad_qwen3_1_7b_glm47_e2e_stage1_2step_stage2_200_stage3_dpo1epoch`

监控范围：
- 对 Stage2 `answer_refresh_data` 运行时资源进行采样。
- 资源监控期间未修改任何训练或推理参数。
- Stage2 请求上限：`inputs.max_samples=200`。
- 为进行 Stage2 优化工作，pipeline 在 Stage2 期间被主动中断。

## 实时采样

| 时间（CST） | Stage2 进度代理指标 | NPU0 actor HBM / AICore | NPU4 teacher HBM / AICore | NPU5 teacher HBM / AICore | NPU6 recall HBM / AICore | NPU7 recall HBM / AICore | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 23:44:32 | actor_posts=27, teacher_posts=8, retrieval_posts=19 | 51888 MB / 0% | 63130 MB / 2% | 63130 MB / 2% | 34829 MB / 0% | 34830 MB / 0% | Actor vLLM、teacher vLLM 和 recall proxy 均已活跃。由于请求较短且间歇串行，该时刻利用率较低。 |
| 2026-07-09 23:45:56 | actor_posts=41, teacher_posts=13, retrieval_posts=28 | 51888 MB / 0% | 63131 MB / 2% | 63130 MB / 2% | 34830 MB / 0% | 34830 MB / 0% | HBM 保持稳定；瞬时 AICore 仍较低，与稀疏/串行的 teacher 调用和短 actor 脉冲一致。 |
| 2026-07-09 23:48:07 | actor_posts=61, teacher_posts=20, retrieval_posts=41 | 51888 MB / 0% | 63131 MB / 2% | 63130 MB / 2% | 34830 MB / 0% | 34830 MB / 0% | 运行中段的资源占用仍稳定；HBM 没有增长，未见泄漏迹象。 |
| 2026-07-09 23:52:39 | actor_posts=98, teacher_posts=32, retrieval_posts=66 | 51888 MB / 0% | 63131 MB / 2% | 63130 MB / 3% | 34829 MB / 0% | 34829 MB / 0% | Teacher 服务是当前活跃瓶颈；actor、teacher 和 recall worker 的内存均保持平稳。 |
| 2026-07-09 23:54:44 | actor_posts=116, teacher_posts=39, retrieval_posts=77 | 51888 MB / 0% | 63130 MB / 2% | 63130 MB / 3% | 34830 MB / 0% | 34829 MB / 0% | 运行中后段采样：HBM 仍稳定；由于 Stage2 发出稀疏的串行调用，瞬时计算利用率仍较低。 |

## 中断时状态

- 按用户要求中断时，Stage2 尚未生成 `refresh_rollouts.jsonl` 或 `answer_distill_pairs.jsonl`。
- 中断后最终的服务日志代理指标：`actor_posts=126`、`teacher_posts=42`、`retrieval_posts=84`。
- 中断后清理检查：未残留 Stage2 vLLM/recall/run_pipeline 进程，端口 `8067`、`8340`、`8130`、`8131`、`8132` 均未监听。

## 分析说明

- NPU0 承载基于合并后 Stage1 checkpoint 的 Stage2 actor vLLM 服务。
- NPU4 和 NPU5 承载 tensor parallel size 为 2 的 GLM4.7 teacher vLLM 服务。
- NPU6 和 NPU7 承载稠密检索 worker，proxy 监听 8130 端口。
- `actor_posts`、`teacher_posts` 和 `retrieval_posts` 根据服务日志计数，仅作为粗粒度实时进度代理指标。
- Stage2 当前按 query 串行执行：先进行带搜索的 actor rollout，再让 teacher 基于已收集证据生成/判断，最后过滤为 DPO pair。各服务同时常驻，但数据生成控制流并非完全并行的 actor-rollout + teacher-rollout pipeline。
